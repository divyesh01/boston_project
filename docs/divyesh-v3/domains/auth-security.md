# Authentication and Security Domain Pack

Treat credentials, tokens, sessions, audit logs, password/MFA flows, inputs, and
external content as hostile boundaries. Separate authentication, authorization, and
property scope. Prove negative cases. Never expose secrets or weaken protected
security behavior. Changes to protected auth/security files require explicit
file-specific owner authorization.


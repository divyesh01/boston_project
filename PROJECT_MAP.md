# 📖 AI Project Map & Edit Impact Guide

## Purpose
This single markdown file gives **any AI model** a quick map of the entire codebase so it doesn't have to scan every file from scratch. It lists:
- The main directories and what they contain.
- Key entry‑point files and their responsibilities.
- Dependencies between front‑end, back‑end and scripts.
- What **will break** if you edit a particular file (simple impact notes).
- Where to find the **official source of truth** for each component.

## How to use it
1. **Read this file first** before any AI‑driven task.
2. Locate the section that matches the area you want to modify (e.g., UI, back‑end function, configuration).
3. Follow the *Impact Checklist* to see which other files may need to be updated.
4. After making changes, **update this map** (or a pull‑request) so future AI runs stay accurate.

## Quick Directory Map
| Directory | Purpose | Important Files |
|-----------|---------|-----------------|
| `src/` | Front‑end React UI | `App.jsx`, `Dashboard.jsx`, `MoneyKept.jsx`, `Login.jsx` |
| `src/orchestrator/` | API-First Multi-Agent Orchestrator | `Orchestrator.js`, `ExecutionLedger.js`, `ActiveActiveRouter.js`, `FallbackPolicy.js`, `PatchApplier.js` |
| `base44/` | Back‑end functions & entities | `entry.js` (various), `RevenueReconciliation.js`, `securityUtils.js` |
| `scripts/` | Build / test helpers | `run-tests.ps1`, `probe-*.mjs` |
| `scripts/orchestrator/` | Multi-agent task & acceptance runners | `run_active_active_acceptance.mjs`, `run_luxury_button_task.mjs`, `probe_providers.mjs` |
| `tests/orchestrator/` | Orchestrator unit & contract test suite | `active_active_router.test.js`, `executive_dashboard.test.js`, `patch_applier.test.js` |
| `public/` | Static assets (HTML, icons) | `index.html` |
| `.env.*` | Local configuration (do **not** commit) | — |

## Impact Checklist (examples)
- **Editing `src/components/dashboard/MoneyKept.jsx`**
  - ✅ Updates displayed profit on dashboard.
  - ⚠️ May affect `src/components/dashboard/RevenueTrend.jsx` if it references the same calculation.
  - 🔧 Remember to run `npm test` after changes.

- **Editing `base44/functions/custom_auth_register/entry.js`**
  - ✅ Changes user registration flow.
  - ⚠️ Affects `src/lib/AuthContext.jsx` (session handling) and `src/pages/Setup.jsx` (first‑run UI).
  - 🔐 Ensure CSRF and password reset logic stay intact.

- **Editing `src/lib/securityUtils.js`** (protected – **do NOT edit** without owner approval)
  - ❌ Would break CSRF protection, rate‑limiting, and audit logs throughout the app.
  - 📌 **Never modify** unless you have explicit permission.

## Updating the Map
Whenever you add a new file, rename a component, or change a dependency, edit this file to keep the map accurate. This keeps future AI agents from needing a full‑repo scan, saving tokens.

---
*Generated by the AI core rules: *Never guess, only prove* and *Always fix from the core*.

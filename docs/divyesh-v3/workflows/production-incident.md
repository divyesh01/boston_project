# Production Incident Workflow

Impact → containment → evidence snapshot → affected-domain analysis → smallest safe
repair → targeted verification → guarded restoration → monitoring → incident report.
Use incident/recovery and only affected domain packs. Do not perform deployment,
rollback, production mutation, or data repair without the authority required for that
action.


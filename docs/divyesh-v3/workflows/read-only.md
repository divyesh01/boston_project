# Read-Only Workflow

Load the kernel and only relevant domain/analysis packs. Inspect and report with
evidence. Do not create a writer or implementation phase. State unknowns and tests
not run. Read-only work is Risk 0 unless it exposes sensitive information or triggers
an external side effect.


# Standard Change Workflow

Owner contract → workflow/state map → relevant NISARG planning → evidence gate →
recon verification → Editor checkpoints → VANSH comparison → targeted/mutation tests
→ regression gate → owner acceptance. Add independent review only when the risk gate
requires it.


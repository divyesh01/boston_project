# Low-Risk Change Workflow

Owner contract → targeted inspection → minimal plan → one writer → targeted test →
diff review → relevant regression → owner acceptance. Use only the planner and domain
packs needed. Escalate if scope reaches protected, destructive, production, schema,
security-policy, or irreversible boundaries.


# Deployment and Operations Domain Pack

Deployment is permitted only when explicitly authorized, gated, and reversible.
Record target, build inputs, artifact, configuration, secrets boundary, preflight,
rollback, post-deploy verification, and owner-visible result. A stale build or unknown
target blocks a live-success claim. Production migrations and data deletion require
separate explicit authorization.

